# Deep Agents — Memory Backend Guide

> **Stub — to be populated from wiki after ingest.**
>
> Source: `wiki/agents/deep-agents-memory-backends.md` (after ingest of `raw/claude-docs/`)
>
> Will cover: `StateBackend` (ephemeral, per-session), `StoreBackend` (persistent across sessions),
> `CompositeBackend` (layered read/write strategy), `FilesystemMiddleware` (file-based persistence),
> when to use each, and migration paths between backends.
