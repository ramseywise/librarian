---
date: 2026-04-11
time: 2026
duration_min: ~60
project: playground
branch: main
status: complete
tests_pass: 476
files_touched: ~160
compacted: false
skills_invoked: [compact-end]
skill_candidates: 0
friction_count: 2
---

# Session — 2026-04-11T2026

## Position
- **Work**: Full repo restructure — resolved git merge conflict, then reorganized `src/librarian/` into clean `src/` + `agents/` split
- **Status**: complete
- **Branch**: main
- **Tests**: 476 passing / 4 deselected (pdfinfo integration, pre-existing env issue)

## Metadata
- **Compacted**: no
- **Key tools**: Bash, Edit, Grep, TodoWrite, Read
- **Files touched**: ~160 (bulk import rewrites across src/, agents/, tests/)
- **Token hotspots**: bulk `find | xargs sed` passes; large `git status --short` dump

## Gotchas

1. **`agents/librarian` was a symlink to `../src/librarian/`** — missed this initially, causing file copies to land in the wrong place. Always `ls -la` a directory before bulk-copying to it.

2. **`src/librarian/` was staged-deleted before the session** — `git status` showed `D src/librarian/...` because a prior session had already removed those files from disk but not committed. `git checkout HEAD -- src/librarian/` restored them cleanly.

3. **`git pull origin main` blocked on untracked `frontend/web/` files** — these were a locally-bootstrapped Next.js 16/React 19 app vs the remote's Next 14/React 18 Bedrock frontend. Backed up to `/tmp/frontend-web-local-backup`, restored as `frontend/web-local/` after the pull.

4. **`tools/utils/config.py` was researcher/presenter config living inside librarian** — the three "utils" layers (`utils/`, `tools/core/`, `tools/utils/`) had a clean hierarchy: `BaseSettings` → `LibrarySettings` → `Settings`. Not duplicates. `tools/utils/config.py` became `agents/core/config/agent_settings.py`; `LibrarySettings` went to `src/librarian/config.py`.

5. **Pydantic v2 `@property` shadowing a field** — `Settings` had `anthropic_api_key` as a `@property` overriding the parent field. Pydantic v2 rejects this at instantiation. Removed the redundant property override.

6. **`src/agents/librarian/eval_harness/metrics/retrieval_eval.py`** — orphan file from an older branch restructure attempt using `playground.src.agents.*` paths. Correct version existed in `src/eval/metrics/retrieval_eval.py`. Deleted orphan.

7. **`mock.patch()` strings need updating too** — sed rewrites only catch `from X import` and `import X` statements. Quoted strings in `patch("agents.librarian.factory._build_llm")` calls were a separate pass.

## Friction signals
- [x] High Bash usage (bash antipatterns: 353 total / 58.83/session — structural, from bulk `find | xargs sed` operations)
- [ ] Repeated tool failures
- [ ] Long test runs
- [x] Excessive compaction (prior sessions noted; this session clean)
- [ ] Heavy write/edit churn

## Attribution notes
- **Primary cause:** `agents/librarian` symlink caused copy ops to land in `src/librarian/` instead of new dir
- **Solved by:** `rm agents/librarian` before creating real `agents/librarian/` directory
- **Why it worked:** removed symlink so subsequent copies went to the real new location
- **Evidence:** `ls agents/librarian/` showed old structure; after `rm`, fresh `mkdir + cp` was clean
- **Could hooks have caught it?** No — this is a filesystem topology issue, not a code quality signal

## Open questions
- [ ] `frontend/web-local/` (Next 16 + Vercel AI SDK) is still untracked — needs a home and a commit once naming is decided
- [ ] `frontend/web/` (Bedrock KB RAG, from origin/main) — should this move to a `client/` dir or separate repo long-term?
- [ ] `src/librarian/factory.py` still has `M` status (modified) — verify content is consistent with new import paths

## Skill candidates
None this session.

## Session insights
1. **Symlink-aware copy check**: before any bulk `cp -r source/. dest/`, `ls -la dest/` to confirm it's a real dir, not a symlink. A pre-flight `[[ -L dest ]]` guard would prevent silent misdirection.
2. **mock.patch() strings are invisible to sed import rewrites** — quoted module paths in `patch()` calls are a consistent blind spot. A post-restructure grep for `patch("agents\.` (old namespace) should be a standard final step in any restructure.
3. **Cartographer bash antipatterns signal is inflated by bulk ops** — the 353 count includes many legitimate `find | xargs sed` restructure operations, not real friction. Cartographer could benefit from a "restructure session" tag to exclude these from trend analysis.

## Next session prompt
Repo restructure is complete and all 476 tests pass. The new layout is: `src/{core,librarian,storage,orchestration,interfaces,eval}/` for library code, `agents/{researcher,presenter,cartographer}/` for runnable agents. Two loose ends: (1) `frontend/web-local/` is untracked Next 16 dev frontend — decide on naming and commit it; (2) `src/librarian/factory.py` shows as modified in git status — worth a quick diff to confirm the import rewrites landed correctly. Start by running `git diff src/librarian/factory.py` to verify, then stage and commit the entire restructure.
